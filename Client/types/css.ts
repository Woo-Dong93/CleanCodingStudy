export enum EColor {
  black = 'black',
  green = 'green',
  blue = 'blue',
}

export enum EFontSize {
  small = 'small',
  middle = 'middle',
  large = 'large',
}

export enum ESize {
  small = 'small',
  middle = 'middle',
  large = 'large',
  auto = 'auto',
}
